# REST API endpoint for job data.
API_BASE_URL=https://your-job-api-endpoint.example.com

# MongoDB connection URI for local use. Adjust if using Atlas or another host.
MONGO_URI=mongodb://localhost:27017/

# Name of the MongoDB database used by the app.
DB_NAME=jobapp

# Name of the collection where applications are stored.
COLLECTION_NAME=applications
